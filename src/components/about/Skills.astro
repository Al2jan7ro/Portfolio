<!-- Skills Section -->
<div class="relative bg-black text-white flex flex-col w-full items-center px-6 py-32 overflow-hidden min-h-screen mt-50">
  <!-- Título -->
  <h1 id="skills-title"
    class="font-BB text-[4rem] sm:text-[5rem] md:text-[6rem] lg:text-[7rem] font-bold z-10 relative text-center">
    SKILLS
  </h1>

  <p id="skills-subtitle"
    class="font-Cg2 text-lg sm:text-xl text-gray-300 mt-6 mb-20 text-center max-w-2xl">
    Destaco por tener un enfoque moderno y un gusto por el detalle visual, integrando diseño, tecnología y trabajo colaborativo.
  </p>

  <!-- Cards grid (6 cards) -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10 w-full max-w-6xl z-10 relative">

    <!-- 1) Tecnologías -->
    <div class="group relative">
      <div class="card-skill relative bg-neutral-900/70 rounded-2xl p-6 shadow-lg  group-hover:shadow-2xl">
        <!-- Glow animado -->
        <div class="absolute inset-0 rounded-2xl pointer-events-none border border-white/30 shadow-[inset_0_1px_8px_rgba(255,255,255,0.6)] animate-gloss"></div>

        <h2 class="flex items-center text-2xl font-bold mb-4 font-Cg2 relative z-10">
          <i class="hgi hgi-stroke hgi-nano-technology font-normal mr-5"></i>
          Tecnologías
        </h2>

        <p class="text-gray-400 font-Cg2 text-base leading-relaxed relative z-10">
          <span class="font-semibold">Mi stack:</span> HTML, CSS, JavaScript, TypeScript, Astro, Tailwind, Node.js, Git/GitHub.<br>
          <span class="font-semibold">Utilizadas:</span> WordPress, Figma.
        </p>
      </div>
    </div>

    <!-- 2) Full Stack (en desarrollo) -->
    <div class="group relative mb-5">
      <div class="card-skill relative bg-neutral-900/70 rounded-2xl p-6 shadow-lg  group-hover:shadow-2xl">
        <div class="absolute inset-0 rounded-2xl pointer-events-none border border-white/30 shadow-[inset_0_1px_8px_rgba(255,255,255,0.6)] animate-gloss"></div>

        <h2 class="flex items-center text-2xl font-bold mb-4 font-Cg2 relative z-10">
          <i class="hgi hgi-stroke hgi-slack font-normal mr-5"></i>
          Full Stack (en desarrollo)
        </h2>

        <p class="text-gray-400 font-Cg2 text-base leading-relaxed relative z-10">
          Capacitado para trabajar con <span class="font-semibold">bases de datos (MySQL)</span>, <span class="font-semibold">TypeScript</span> y <span class="font-semibold">Java</span>,
          desarrollando en un entorno Full-stack y orientado a DevOps.
        </p>
      </div>
    </div>

    <!-- 3) Trabajo en Equipo -->
    <div class="group relative">
      <div class="card-skill relative bg-neutral-900/70 rounded-2xl p-6 shadow-lg  group-hover:shadow-2xl">
        <div class="absolute inset-0 rounded-2xl pointer-events-none border border-white/30 shadow-[inset_0_1px_8px_rgba(255,255,255,0.6)] animate-gloss"></div>

        <h2 class="flex items-center text-2xl font-bold mb-4 font-Cg2 relative z-10">
          <i class="hgi hgi-stroke hgi-user-group font-normal mr-5"></i>
          Trabajo en Equipo
        </h2>

        <p class="text-gray-400 font-Cg2 text-base leading-relaxed relative z-10">
          Experiencia en proyectos colaborativos aplicando <span class="font-semibold">metodologías ágiles</span> (Scrum, Kanban),
          uso de <span class="font-semibold">Git</span> y control de versiones, además de prácticas de <span class="font-semibold">CI/CD</span>.
        </p>
      </div>
    </div>

    <!-- 4) Experiencia -->
    <div class="group relative">
      <div class="card-skill relative bg-neutral-900/70 rounded-2xl p-6 shadow-lg  group-hover:shadow-2xl">
        <div class="absolute inset-0 rounded-2xl pointer-events-none border border-white/30 shadow-[inset_0_1px_8px_rgba(255,255,255,0.6)] animate-gloss"></div>

        <h2 class="flex items-center text-2xl font-bold mb-4 font-Cg2 relative z-10">
          <i class="hgi hgi-stroke hgi-document-validation font-normal mr-5"></i>
          Experiencia
        </h2>

        <p class="text-gray-400 font-Cg2 text-base leading-relaxed relative z-10">
          Más de <span class="font-semibold">2 años</span> desarrollando soluciones web y sistemas personalizados, con foco en automatización, usabilidad y despliegue.
        </p>
      </div>
    </div>

    <!-- 5) Formación Académica -->
    <div class="group relative">
      <div class="card-skill relative bg-neutral-900/70 rounded-2xl p-6 shadow-lg  group-hover:shadow-2xl">
        <div class="absolute inset-0 rounded-2xl pointer-events-none border border-white/30 shadow-[inset_0_1px_8px_rgba(255,255,255,0.6)] animate-gloss"></div>

        <h2 class="flex items-center text-2xl font-bold mb-4 font-Cg2 relative z-10">
          <i class="hgi hgi-stroke hgi-dashboard-circle-edit font-normal mr-5"></i>
          Formación
        </h2>

        <p class="text-gray-400 font-Cg2 text-base leading-relaxed relative z-10">
          Ingeniero de Sistemas (en proceso de titulación). Universidad <span class="font-semibold">UNEFA (2020 - 2025), Venezuela</span>. Especializado en desarrollo de sistemas web.
        </p>
      </div>
    </div>

    <!-- 6) Enfoque -->
    <div class="group relative">
      <div class="card-skill relative bg-neutral-900/70 rounded-2xl p-6 shadow-lg group-hover:shadow-2xl">
        <div class="absolute inset-0 rounded-2xl pointer-events-none border border-white/30 shadow-[inset_0_1px_8px_rgba(255,255,255,0.6)] animate-gloss"></div>

        <h2 class="flex items-center text-2xl font-bold mb-4 font-Cg2 relative z-10">
          <i class="hgi hgi-stroke hgi-search-focus font-normal mr-5"></i>
          Enfoque
        </h2>

        <p class="text-gray-400 font-Cg2 text-base leading-relaxed relative z-10">
          Mi trabajo combina <span class="font-semibold">tecnología, diseño y usabilidad</span> para crear experiencias digitales modernas, accesibles y con impacto real.
        </p>
      </div>
    </div>

  </div>
</div>


<!-- Scripts -->
<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  import SplitText from "gsap/SplitText";

  gsap.registerPlugin(ScrollTrigger, SplitText);

  document.addEventListener("astro:page-load", () => {
    let ctx = gsap.context(() => {
      // 🔹 SplitText en título y subtítulo
      const titleSplit = new SplitText("#skills-title", { type: "chars" });
      const subtitleSplit = new SplitText("#skills-subtitle", { type: "words" });

      gsap.from(titleSplit.chars, {
        opacity: 0,
        y: 60,
        duration: 1,
        stagger: 0.05,
        ease: "power3.out",
        scrollTrigger: {
          trigger: "#skills-title",
          start: "top 80%",
        },
      });

      gsap.from(subtitleSplit.words, {
        opacity: 0,
        y: 40,
        duration: 0.8,
        stagger: 0.08,
        ease: "power2.out",
        scrollTrigger: {
          trigger: "#skills-subtitle",
          start: "top 85%",
        },
      });

          gsap.utils.toArray<HTMLElement>(".card-skill").forEach((card, i) => {
            gsap.from(card, {
              y: 50,
              opacity: 0,
              duration: 0.9,
              delay: i * 0.15,
              ease: "power2.out",
              scrollTrigger: {
                trigger: card,
                start: "top 85%",
              },
            });
          });

    });

    return () => ctx.revert();
  });
</script>
